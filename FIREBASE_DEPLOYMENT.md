# Firebase デプロイ手順

このドキュメントでは、Firebaseセキュリティルールとインデックスをデプロイする手順を説明します。

## 前提条件

- Firebase プロジェクトが作成済み
- Firebase CLI がインストール済み
- Firebaseにログイン済み

---

## 📦 準備されたファイル

以下のファイルが準備されています：

```
soramoyou/
├── firebase.json              # Firebase設定ファイル
├── .firebaserc                # プロジェクト設定
├── firestore.rules            # Firestoreセキュリティルール
├── firestore.indexes.json     # Firestoreインデックス定義
├── storage.rules              # Storageセキュリティルール
└── firebase-deploy.sh         # デプロイスクリプト（自動化）
```

---

## 🚀 方法1: デプロイスクリプトを使用（推奨）

### ステップ1: Firebaseプロジェクト作成（初回のみ）

1. [Firebase Console](https://console.firebase.google.com/) にアクセス
2. 「プロジェクトを追加」をクリック
3. プロジェクト名を入力（例: `soramoyou`）
4. プロジェクトIDをメモ（例: `soramoyou-12345`）

### ステップ2: Firebase CLIのインストール（未インストールの場合）

```bash
# npmを使用してインストール
npm install -g firebase-tools

# バージョン確認
firebase --version
```

### ステップ3: Firebaseにログイン

```bash
firebase login
```

ブラウザが開いて、Googleアカウントでログインします。

### ステップ4: デプロイスクリプトを実行

```bash
# プロジェクトディレクトリに移動
cd /path/to/soramoyou

# スクリプトを実行（プロジェクトIDを指定）
./firebase-deploy.sh your-project-id

# または、.firebasercに設定済みの場合
./firebase-deploy.sh
```

**スクリプトの動作:**
1. ログイン状態を確認
2. デプロイ対象ファイルを表示
3. 確認プロンプト（y/N）
4. Firestoreルールをデプロイ
5. Firestoreインデックスをデプロイ
6. Storageルールをデプロイ

---

## 🔧 方法2: 手動デプロイ

### ステップ1: プロジェクトIDを設定

```bash
# .firebasercを編集してプロジェクトIDを設定
cat > .firebaserc <<EOF
{
  "projects": {
    "default": "your-project-id"
  }
}
EOF
```

### ステップ2: 個別にデプロイ

#### Firestoreセキュリティルール

```bash
firebase deploy --only firestore:rules
```

#### Firestoreインデックス

```bash
firebase deploy --only firestore:indexes
```

#### Storageセキュリティルール

```bash
firebase deploy --only storage
```

#### すべてを一度にデプロイ

```bash
firebase deploy --only firestore,storage
```

---

## 🎯 方法3: Firebase Console（GUI）

CLIが使えない場合は、Firebase Consoleから手動でデプロイできます。

### Firestoreセキュリティルール

1. [Firebase Console](https://console.firebase.google.com/) を開く
2. プロジェクトを選択
3. **Firestore Database** > **ルール** を開く
4. `firestore.rules` の内容をコピー＆ペースト
5. **公開** をクリック

### Storageセキュリティルール

1. Firebase Console で **Storage** > **ルール** を開く
2. `storage.rules` の内容をコピー＆ペースト
3. **公開** をクリック

### Firestoreインデックス

1. Firebase Console で **Firestore Database** > **インデックス** を開く
2. `firestore.indexes.json` の各インデックスを手動で作成
3. または、アプリを実行してエラーメッセージ内のリンクから自動作成

---

## ✅ デプロイ後の確認

### 1. Firebase Consoleで確認

#### Firestoreルール
```
Firestore Database > ルール
```
- ルールが更新されているか確認
- 最終更新日時をチェック

#### Storageルール
```
Storage > ルール
```
- ルールが更新されているか確認

#### Firestoreインデックス
```
Firestore Database > インデックス
```
- 7つのインデックスが作成されているか確認
- ステータスが「有効」になっているか確認

### 2. コマンドラインで確認

```bash
# プロジェクト情報を表示
firebase projects:list

# 現在のプロジェクトを表示
firebase use
```

---

## 🧪 テスト

### Firestoreルールのテスト

Firebase Consoleの**ルールプレイグラウンド**でテストできます：

```
Firestore Database > ルール > ルールプレイグラウンド
```

**テストケース例:**

```javascript
// ケース1: 認証済みユーザーが自分の投稿を作成（成功するはず）
operation: create
path: /databases/(default)/documents/posts/test-post-123
auth: { uid: "user123" }
data: {
  postId: "test-post-123",
  userId: "user123",
  images: [{ url: "https://example.com/image.jpg" }],
  visibility: "public",
  likesCount: 0,
  commentsCount: 0,
  createdAt: <server_timestamp>,
  updatedAt: <server_timestamp>
}
// 結果: Allow

// ケース2: 未認証ユーザーが投稿を作成（失敗するはず）
operation: create
path: /databases/(default)/documents/posts/test-post-456
auth: null
data: { ... }
// 結果: Deny
```

### Storageルールのテスト

```
Storage > ルール > ルールプレイグラウンド
```

---

## 🔄 ルール更新のワークフロー

ルールを変更した場合の推奨ワークフロー：

### 1. ローカルで変更

```bash
# firestore.rules を編集
vim firestore.rules

# storage.rules を編集
vim storage.rules
```

### 2. Gitにコミット

```bash
git add firestore.rules storage.rules
git commit -m "update: Firebaseセキュリティルールを更新"
git push
```

### 3. デプロイ

```bash
./firebase-deploy.sh
```

### 4. Firebase Consoleで確認

ルールが正しく反映されているか確認

---

## 🐛 トラブルシューティング

### エラー: "Authentication Error"

**原因:** Firebase CLIにログインしていない

**解決方法:**
```bash
firebase login
```

### エラー: "Project not found"

**原因:** `.firebaserc` のプロジェクトIDが間違っている

**解決方法:**
1. Firebase ConsoleでプロジェクトIDを確認
2. `.firebaserc` を更新
```bash
cat > .firebaserc <<EOF
{
  "projects": {
    "default": "correct-project-id"
  }
}
EOF
```

### エラー: "Permission denied"

**原因:** Firebaseプロジェクトへのアクセス権限がない

**解決方法:**
1. Firebase Consoleでプロジェクトにアクセスできるか確認
2. プロジェクトのオーナーまたは編集者権限があるか確認

### エラー: "Deployment failed"

**原因:** ルールファイルに構文エラーがある

**解決方法:**
1. ルールファイルの構文を確認
2. Firebase Consoleのルールエディタでエラーをチェック
3. 修正後、再度デプロイ

### インデックスの作成に時間がかかる

**正常な動作:** インデックスの作成には数分〜数十分かかる場合があります

**確認方法:**
```
Firebase Console > Firestore Database > インデックス
```
ステータスが「作成中」から「有効」に変わるまで待機

---

## 📝 重要な注意事項

### セキュリティ

- ✅ **本番環境にデプロイする前に、必ずテストしてください**
- ✅ ルールを変更した場合は、必ず動作確認を行う
- ✅ デフォルト拒否（Deny by Default）の原則を守る
- ⚠️ テストモードのルールは本番環境で使用しない

### バックアップ

- 重要な変更の前に、現在のルールをバックアップ
- Firebase Consoleでルールのバージョン履歴を確認可能

### CI/CD

本番環境では、CI/CDパイプラインでの自動デプロイを推奨：

```bash
# GitHub Actionsの例
- name: Deploy to Firebase
  run: |
    npm install -g firebase-tools
    firebase deploy --only firestore,storage --token ${{ secrets.FIREBASE_TOKEN }}
```

---

## 🎉 デプロイ完了後の次のステップ

1. **Firebaseサービスの有効化**
   - Authentication: メール/パスワード認証を有効化
   - Firestore: データベースを作成（ロケーション: asia-northeast1）
   - Storage: ストレージを作成

2. **GoogleService-Info.plistの配置**
   - Firebase ConsoleからダウンロードしてXcodeプロジェクトに追加

3. **アプリの動作確認**
   - シミュレーターで実行
   - 投稿作成、検索などの機能をテスト

詳細は `SETUP_GUIDE.md` を参照してください。

---

## 参考資料

- [Firebase CLI リファレンス](https://firebase.google.com/docs/cli)
- [Firestore セキュリティルール](https://firebase.google.com/docs/firestore/security/get-started)
- [Storage セキュリティルール](https://firebase.google.com/docs/storage/security)
- [Firestore インデックス](https://firebase.google.com/docs/firestore/query-data/indexing)
